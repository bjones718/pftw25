 
function setup() {
  createCanvas(400, 400);
  background(0);
  ellipse(200, 200, 150, 150);
  textAlign(CENTER, CENTER);
  textSize(20);

}
function setup() {
  background('#4c0099');
  ellipse(200, 200, 300, 300);
}

